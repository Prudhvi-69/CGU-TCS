Input : "Random Word Faaahh Cgu Global"
len(str) 
int count=0;
for(i = 0 ; i< strlen(str) ; i++)    // for(i =0 ; str[i] ! = '\0' ; i++)
{ 
	if( str[i] ! = ' '  &&  str[i+1] == ' ' )
	{
		count ++;
 	}
} 
int last_index = strlen(str)-1;
if(str[last_index] != ' ')
	count++:
printf("%d", count); // 5 






Output1 : 5 

Output2 : Cgu

ASCII value of space is : 32 
ASCII values of range (A to Z) : 65 to 90
ASCII values of range (a to z) : 97 to 122 
ASCII values of range (0 to 9) : 48 to 57 
ASCII value of asterisk (*): 42










input1 : "copy coffee clock trees alcohol"
output1 : copy

input2 : "chemistry organic inorganic magic carbon"
output2 : magic

input3 : "history mobile upsc entrance methanol"
output3 : upsc 

*_* You can check the length of the each word in the string and compare it with the other one

-_- When you are comparing two strings, smallest one will be stored in a separate variable

(*_*) : WHY TO WASTE TIME SIRRRRRRRRRRRRRRRR (SIRR JUST ONE HOURR)






input2 : "chemistry organic inorganic magic carbon "
output2 : magic



char str[ ] = "chemistry organic inorganic magic carbon" 
String smallest = str;  // char smallest[70] = str; 
String current = "";	// char current[90] = "";      // char *current ; 
for(i = 0 ; str[i] != '\0' ; i++) 
{ 
	if(str[i] != ' ')
	{
 		current = strcat(current, str[i]); //  carbon 
	} 
	else
	{
 		if( strlen(smallest) > strlen(current) )  // 5>6   
		{
			smallest = current;     // magic 
		} 
		current="";
	} 
}
printf(" %s ", smallest);   //magic 








(*_*) : Pointers always do store address of variables 

int a;
a = 69;
int *b;
b = &a;
*b = 96;  // this is going to directly update the value inside the original variable where pointer is pointing
int **c;  // double pointer : Store address of single pointer
c = &b;


** Whenever you are accessing a pointer using * and its name after it like (*ptr) it is going to the address it is storing and access the value in that address.

char *c; : Character pointer is a Dynamic String, which is going to not have any limit on the size of the String (Arrays of Char type) 



























