Input :     "Decimal Binary    Fraction Faaah Word Hello Drum Fridge Fans Yes Sir   Pen"

Output :  12 
# This code is going to work only when there is a exactly one space between every two words 
str = input()  
count = 0
for i in range( len(str) ) : 
	if str[ i ] == ' ': 
		count += 1    # 16 

count += 1 
print( count ) # 17 


#this will work in all cases and scenarios where there are multiple spaces also 
count = 0 
for i in range( len(str)-1 ): 
	if str[i] != ' ' and str[i+1] == ' ': 
		count += 1 

if str[ -1 ] != ' ' : 
	count += 1 
print( count ) 


	


OOPS for Robotics

Polymorphism 
--------------> Compile Time Polymorphism 
--------------> Run Time Polymorphism 

Compile Time Polymorphism : We (users or coders) and the compiler will be able to understand that we have performed a polymorphism and we will be able to identify it at the compile time.

Can be acted on functions, and it is known as Function Overloading.....

area(10)   // square 
area(4,7)  // rectangle 
area(10.0) // circle 
area(1.1, 3.2) // triangle 




















